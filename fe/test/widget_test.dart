import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts with SplashScreen', (WidgetTester tester) async {
    // Placeholder test — update after full integration
    expect(true, isTrue);
  });
}
